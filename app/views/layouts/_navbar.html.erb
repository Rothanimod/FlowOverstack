<nav class="navbar navbar-inverse">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "flowOVERSTACK", root_path, class: 'navbar-brand' %>
    </div>
    <!-- Ransack search form  -->
    <div class="form-group">
      <%= search_form_for @q, html: {class: "navbar-form navbar-right"} do |que| %>
      <%= que.search_field :title_or_description_cont, :class => 'form-control', :placeholder => 'Titulo o palabras clave' %>
      <%= que.submit 'Q', :class => 'btn btn-default'%>
    </div>
    <%end%>
    <!-- Ransack form end -->

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li > <%= link_to 'Nueva Pregunta', new_question_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email.split('@')[0] %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li> <%= link_to 'Cerrar Sesion', destroy_user_session_path, method: :delete %></li>
            <%else%>
              <li> <%= link_to 'Iniciar Sesion', new_user_session_path %> </li>
              <li> <%= link_to 'Registrarse', new_user_registration_path %></li>
          </ul>
        </li>
        <%end%>
      </ul>


    </div><!-- /.navbar-collapse -->

  </div><!-- /.container-fluid -->
</nav>
